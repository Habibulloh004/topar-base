name: Build Desktop App

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: read

concurrency:
  group: desktop-build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            artifact_name: topar-desktop-macos
            artifact_paths: |
              topar-desktop/src-tauri/target/release/bundle/dmg/*.dmg
              topar-desktop/src-tauri/target/release/bundle/macos/*.app
          - os: windows-latest
            artifact_name: topar-desktop-windows
            artifact_paths: |
              topar-desktop/src-tauri/target/release/bundle/msi/*.msi
              topar-desktop/src-tauri/target/release/bundle/nsis/*.exe

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: topar-desktop/package-lock.json

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Cache Rust builds
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            topar-desktop/src-tauri -> target

      - name: Install dependencies
        working-directory: topar-desktop
        run: npm ci

      - name: Prepare parser runtime (macOS/Linux)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          set -euo pipefail
          cd topar-desktop/src-tauri/parser_engine
          python -m venv --copies venv
          ./venv/bin/python -m pip install --upgrade pip
          ./venv/bin/python -m pip install -r requirements.txt
          PLAYWRIGHT_BROWSERS_PATH=0 ./venv/bin/python -m playwright install chromium

      - name: Prepare parser runtime (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          Set-StrictMode -Version Latest
          cd topar-desktop/src-tauri/parser_engine
          python -m venv venv
          .\venv\Scripts\python -m pip install --upgrade pip
          .\venv\Scripts\python -m pip install -r requirements.txt
          $env:PLAYWRIGHT_BROWSERS_PATH = "0"
          .\venv\Scripts\python -m playwright install chromium

      - name: Build production desktop app
        working-directory: topar-desktop
        run: npm run tauri:build

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ matrix.artifact_paths }}
          if-no-files-found: error
